import type { Certificate } from '@/types/portfolio';

// Confirmed empty — no professional certifications on file yet. The
// cloud-computing workshop cert lives under Events (evt-007) since it's a
// workshop attendance, not an issued credential. CertificatesSection
// renders nothing when this array is empty.
export const certificates: Certificate[] = [];
